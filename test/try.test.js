describe('Main test', () => {
  describe('Test add function', () => {
    test('should add two numbers', () => {
      expect(1 + 2).toBe(3);
    });
    test('should multiply two numbers', () => {
      expect(6 * 2).toBe(12);
    });
  });
});
